<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <div class="nanbar-header">
      <a class="navbar-brand" href="/">测试结果数据平台</a>
    </div>

    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <!-- 加入语言切换入口  -->
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" >
            语言切换
            <b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <li><%= link_to "中文版", :controller => controller_name, :action => action_name, :locale => "zh-CN" %></li>
            <li><%= link_to "English", :controller => controller_name, :action => action_name, :locale => "en" %></li>
          </ul>
        </li>
        <!-- 注册登录情况  -->
        <% if !current_user %>
          <li><%= link_to("注册", new_user_registration_path) %></li>
          <li><%= link_to("登入", new_user_session_path) %></li>
        <% else %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" >
              <!-- Hi!, <%= current_user.email %> -->
              <!-- 多语言词汇内嵌变量  -->
              <%= t("hello", :name =>current_user.email) %>
              <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <% if current_user.is_admin? %>
                <li>
                  <%= link_to("Admin 选项", admin_test_reports_path) %>
                </li>
              <% end %>
              <!-- 个人资料显示 -->
              <li>
                <%= link_to("我的个人资料", user_path) %>
              </li>
              <!-- 修改个人资料 -->
              <li>
                <%= link_to("修改个人资料", edit_user_path) %>
              </li>
              <li><%= link_to("登出", destroy_user_session_path, method: :delete) %></li>
            </ul>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
